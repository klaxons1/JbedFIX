org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=false
# The decompiled sources reference R constants from switch/if chains;
# keep them final so they remain compile-time constants.
android.nonFinalResIds=false
